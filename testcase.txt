#include <stdio.h>
#include<stdlib.h>

struct Node {
    int data;
    struct Node* next;
};

// Function with missing semicolons
void processData(int value) {
    int result = value * 2
    printf("Processing value: %d\n", value)
    
    if (value > 0) {
        printf("Positive value detected\n");
        int isEven = (value % 2 == 0)
        if (isEven) {
            printf("Value is even\n")
        }
    }
    
    // Extra semicolon after function call
    calculateSum(value);;
}

int calculateSum(int num) {
    int sum = 0;
    // Missing semicolon in for loop initialization
    for(int i = 0 i <= num; i++) {
        sum += i;
    }
    return sum;
}

void displayNode(struct Node* node) {
    if (node != NULL) {
        // Extra semicolon after if statement
        if (node->data > 0);{
            printf("Node data: %d\n", node->data)
        }
    }
}

int main() {
    // Create a sample node
    struct Node* node = (struct Node*)malloc(sizeof(struct Node))
    node->data = 42;
    node->next = NULL
    
    // Call functions
    processData(10);
    displayNode(node);
    
    // Free memory
    free(node);
    
    return 0;
}