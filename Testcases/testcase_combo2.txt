#include <stdio.h>
#include <stdlib.h>

int calculate(int x, int y) {
    if (y == 0 { // Missing closing parenthesis
        printf("Error: Division by zero attempt.\n");
        return 0;
    }
    return x / y;; // Extra semicolon
}

int main() {
    char name[50];
    int result;

    printf("Enter your name: ");
    gets(name); // Unsafe function gets

    result = calculate(10, 0);
    printf("Result: %d\n", result);
    // Missing closing brace for main